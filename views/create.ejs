<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Spotify Playlist Filterer</title>
    <link rel="shortcut icon" href="/images/spotify.webp">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
    <link href="style.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
</head>

<body>
    <input class="btn btn-primary btn-lg" type="button" onclick="window.location='/logout'" value="Logout">
    <input id="allPlaylists" class="btn btn-primary btn-lg" type="button" onclick="window.location='/playlists'" value="Playlists">
    <input id="create" class="btn btn-primary btn-lg" type="button" onclick="window.location='/create'" value="Create Playlist">
    <div class="container">
        <div id="filterSection">
            <h1 class = "projectName"><img src="/images/spotify.webp" width="50" height="50" style="padding: 5px"></img>    Spotify Playlist Filterer</h1>
            <div >
                <form action="" method="post">
                    <div class="mb-5">
                        <label for="playlistEnter" class="form-label headerText">Enter playlist URL you want to filter from Spotify (e.g. https://open.spotify.com/playlist/1X4m3caIi1cAXAC5gUO0Ny) </label>
                        <input name="playlistEnter" id="playlistEnter" class="form-control">
                    </div>

                    <div class="mb-5">
                        <label for="genresEnter" class="form-label headerText" class="headerText">Enter genre(s) of interest separated by commas (genres can be found <a href = "https://everynoise.com/everynoise1d.cgi?scope=all">here</a>, e.g. pop,rap). Leaving this empty will consider all genres.</label>
                        <input name="genresEnter" id="genresEnter" class="form-control">
                        </select>
                    </div>

                    <div class="mb-5">
                        <label for="artistsEnter" class="form-label headerText" class="headerText">Enter artist(s) of interest separated by commas (e.g. Justin Bieber,Drake,Billie Ellish). Leaving this empty will consider all artists.</label>
                        <input name="artistsEnter" id="artistsEnter" class="form-control">
                        </select>
                    </div>

                    <div class="mb-5">
                        <label for="yearEnter" class="form-label headerText" class="headerText">Enter year (e.g. 2019, this means all songs that were added from 2019 and beyond in the above playlist will be added to the new playlist). <br>Leaving this empty will consider all years.</br></label>
                        <input name="yearEnter"  id="yearEnter" class="form-control">
                        </select>
                    </div>

                    <div class="mb-5">
                        <label for="playlistName" class="form-label headerText" class="headerText">Enter new playlist name (Default: New Playlist) </label>
                        <input name="playlistName" id="playlistName" class="form-control">
                        </select>
                    </div>
                    <div style="text-align: center">
                            <input id="enter" class="btn btn-primary btn-lg" type="submit"  value="Filter playlist">
                            <div class="errorMessages">
                                <% if (locals.errorMessage) %>
                                    <%- locals.errorMessage %>
                            </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>

</html>